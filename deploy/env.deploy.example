# Copiar a deploy/.env.deploy (NO commitear el real). Lo usa deploy/deploy.sh
# para saber con qué URL de API compilar el admin y la landing (Vite la
# inyecta en el build — hay que reconstruir si esto cambia).
VITE_API_URL=https://tudominio.com/api
